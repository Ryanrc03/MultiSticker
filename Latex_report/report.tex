\documentclass[10pt,twocolumn,letterpaper]{article}

\usepackage{cvpr}
\usepackage{times}
\usepackage{epsfig}
\usepackage{graphicx}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{booktabs}
\usepackage{xcolor}

\usepackage[breaklinks=true,bookmarks=false]{hyperref}

\cvprfinalcopy

\def\cvprPaperID{****}
\def\httilde{\mbox{\tt\raisebox{-.5ex}{\symbol{126}}}}
\newcommand{\best}[1]{\textcolor{red!75!black}{\textbf{#1}}}
\newcommand{\second}[1]{\textcolor{blue!75!black}{\textbf{#1}}}

\setcounter{page}{1}
\begin{document}

%%%%%%%%% TITLE
\title{MultiSticker: Intent-Guided Multimodal Sticker Retrieval\\with Dialogue Memory}

\author{Rongcheng Li, Xinyou\\
COMP 646: Deep Learning for Vision and Language final project report\\
Department of Computer Science, Rice University\\
{\tt\small rl182@rice.edu, xy68@rice.edu}
}

\maketitle

%%%%%%%%% ABSTRACT
\begin{abstract}
Stickers are widely used in messaging platforms to express affect, humor and
intent. Retrieving the right sticker for a dialogue turn is challenging because
(i)~the candidate bank is large and dominated by animated formats, (ii)~the
correct reply depends on long-range conversational state rather than just the
last turn, and (iii)~each context typically has only one observed positive even
though many semantically equivalent stickers exist. We present
\textbf{MultiSticker}, an intent-guided sticker retrieval system that combines
recent dialogue context, retrieved long-term session memory, and an
LLM-generated reply intent with a frozen-or-adapted OpenCLIP sticker bank. The
paper is organized around two experiments. First, we test whether dialogue
memory is useful by comparing frozen CLIP and a learned retrieval head with and
without retrieved memory. Frozen CLIP already retrieves semantically related
stickers (Group@10 = 0.9078), but fails at exact sticker selection
(Exact@30 = 0.0028); adding a learned head and retrieved memory raises
Exact@30 to 0.1263. Second, we ask which parameter-efficient SFT direction is
best: text-side LoRA, image-side LoRA, or dual LoRA. On the completed
medium-scale U-Sticker run, dual LoRA gives the best trained-model semantic
Group@5/10 performance, while image-side LoRA gives the strongest exact top-5
ranking. We argue that for a chat sticker panel, semantic Group@5/10 is the
right north-star metric and exact retrieval should be treated as a secondary
within-group ranking diagnostic.
\end{abstract}

\begin{figure*}[t]
\centering
\includegraphics[width=0.98\textwidth]{figure1.pdf}
\caption{\textbf{MultiSticker at a glance.} A real validation dialogue is
converted into three retrieval signals: local dialogue context, retrieved
long-term memory, and an LLM-generated reply intent. The dual-LoRA OpenCLIP
retriever projects these signals into the sticker embedding space and returns
a ranked panel of real sticker candidates; animated \texttt{.webm} and
\texttt{.gif} media are shown by their first frame.}
\label{fig:teaser}
\end{figure*}

%%%%%%%%% BODY TEXT
\section{Introduction}

When users reply with stickers in a chat, the choice depends on local
context (what was just said), long-range state (who is talking, prior tone,
recurring topics) and the communicative \emph{intent} behind the reply
(e.g.\ gratitude, encouragement, teasing). A retrieval system that only looks
at the last few turns frequently selects stickers with the wrong affect or the
wrong recurring style for a given user stream. Conversely, a system that uses
only image-side similarity ignores the dialogue entirely.

We focus on the U-Sticker corpus, which contains real multilingual chats
spanning crypto, gaming and social-domain channels. Several properties make
sticker retrieval different from generic image--text retrieval. First, the
candidate set is sparse and animated: 42{,}319 decodable stickers across
\texttt{.png}, \texttt{.gif} and \texttt{.webm}. Second, supervision is
\emph{single-positive}: each turn records the one sticker the user actually
sent, even though many other stickers in the bank are equally valid. Third,
turns frequently contain code-switching and chat slang outside CLIP's
pre-training distribution.

We build on the IGSR-style design~\cite{showandtell} of using a CLIP backbone
plus a small learned head, and extend it in three directions:
\begin{itemize}
\item A retrieved \emph{long-term memory} stream from previous sessions of the
      same user/file, encoded by multilingual~E5~\cite{wang2022e5}.
\item An LLM-generated \emph{reply intent} stream that abstracts away from the
      exact sticker id and describes the communicative function of the reply.
\item A unified pipeline that handles \texttt{.png}, \texttt{.gif} and
      \texttt{.webm} via per-frame OpenCLIP encoding and mean pooling, and
      supports parameter-efficient adaptation through LoRA~\cite{hu2021lora}
      on the text side, image side, or both.
\end{itemize}

\noindent\textbf{Contributions.}
(1)~A memory ablation showing that retrieved long-term dialogue memory mainly
improves fine-grained sticker-id selection, not only coarse semantic matching.
(2)~A modality-SFT study comparing text-side, image-side, and dual LoRA on the
same all-media sticker bank, showing that dual LoRA is most balanced for
semantic panel retrieval while image LoRA is best for exact top-5 ranking.
(3)~A reusable, self-contained MultiSticker codebase including data manifest
construction, memory retrieval, OpenCLIP wrapping, multi-frame decoding,
LoRA-capable training modes, and SLURM-friendly run scripts.

%-------------------------------------------------------------------------
\section{Related Work}

\noindent\textbf{Vision--language retrieval.}
CLIP~\cite{radford2021clip} popularised dual-encoder image--text retrieval,
and OpenCLIP~\cite{ilharco2021openclip} provides open-weight reproductions
trained on LAION-class corpora. Our system uses ViT-B/32 with the
\texttt{laion2b\_s34b\_b79k} weights as the backbone for both the text and
image branches.

\noindent\textbf{Sticker / meme retrieval.}
Sticker reply suggestion has been studied as a multimodal retrieval and
ranking problem~\cite{laddha2020understanding,gao2020learning}. Intent-guided
sticker retrieval (IGSR)~\cite{zhang2022igsr} adds an explicit intent
classification head and a group prior. We adopt this template and add long-term
memory and parameter-efficient adaptation.

\noindent\textbf{Memory-augmented dialogue.}
Long-term dialogue memory has been used to condition open-domain
chatbots~\cite{xu2022long}. We use it specifically to construct a third query
stream for retrieval rather than to generate text. Our memory encoder is
multilingual~E5~\cite{wang2022e5}, chosen for code-switched chat data.

\noindent\textbf{Parameter-efficient fine-tuning.}
LoRA~\cite{hu2021lora} adds low-rank update matrices to selected linear
layers, leaving the original weights frozen. We inject LoRA into OpenCLIP's
\texttt{out\_proj}, \texttt{c\_fc} and \texttt{c\_proj} modules through
PEFT~\cite{mangrulkar2022peft}, and isolate text-side, image-side and dual
adaptation through name-based parameter masking.

%-------------------------------------------------------------------------
\section{Method}

\subsection{Pipeline overview}

\begin{figure*}[t]
\centering
\includegraphics[width=0.96\textwidth]{pipeline_overview.pdf}
\caption{End-to-end MultiSticker pipeline. Raw U-Sticker JSON is split into
12-hour sessions; an LLM produces a per-session memory and a per-sample
intent label/text; multilingual~E5 retrieves the top-3 historical sessions
for the current turn; OpenCLIP independently encodes the context, memory
and intent strings; the \texttt{IntentGuidedRetriever} head projects their
concatenation into the CLIP image space and ranks the sticker bank by
cosine similarity. An auxiliary intent classifier predicts the sticker's
group and supplies a prior for the two-stage rerank.}
\label{fig:pipeline}
\end{figure*}

For each turn $t$ where the user sent a sticker $s^\star_t$, we form three
text inputs: the immediate \emph{context} $c_t$ (up to 12 prior turns),
the retrieved \emph{memory} $m_t$ (concatenation of the top-3 prior session
summaries), and the LLM \emph{intent} $i_t$ (a short description of what the
sticker should express). These are encoded independently by the OpenCLIP
text encoder $f_T$. Sticker images $s$ are encoded by the OpenCLIP image
encoder $f_I$ (with mean-pooling over frames for animated formats). The
trainable head $g_\theta$ projects the concatenation into the image space:
\begin{equation}
\mathbf{q}_t = \mathrm{norm}\Bigl(g_\theta\bigl(
  [f_T(c_t);\,f_T(m_t);\,f_T(i_t)]
\bigr)\Bigr).
\end{equation}
Retrieval logits are
$\mathbf{z}_t = \mathbf{q}_t\,\mathbf{B}^{\top}/\tau$,
where $\mathbf{B}\!\in\!\mathbb{R}^{N\times d}$ is the (optionally adapted)
OpenCLIP image bank and $\tau$ is a learned temperature. A second head
predicts the sticker's intent group $y_t$, and we minimise
\begin{equation}
\mathcal{L} = \mathrm{CE}(\mathbf{z}_t, s^\star_t) +
              \lambda \,\mathrm{CE}(\mathbf{z}^{\mathrm{int}}_t, y_t),
\quad \lambda{=}0.2.
\end{equation}

\subsection{Long-term memory retrieval}

Each raw chat file is split into sessions by a 12-hour inactivity gap. For
sample $t$, we collect up to 8 prior sessions from the same file. Their
LLM-generated session summaries are encoded with the E5 \texttt{passage:}
prefix; the current session text is encoded with the \texttt{query:} prefix.
After L2 normalisation, the dot products rank the prior sessions and we keep
the top-3. The selected memory texts are concatenated and stored as $m_t$,
then encoded by OpenCLIP at training time. Crucially, memory enters the model
through the \emph{text} encoder, not as a separate vector, which lets the
retrieval head learn to reweight memory against the local context.

\subsection{LLM annotation}

We annotate every session with a $\le$~4-sentence
\texttt{session\_memory\_text} (used as the passage corpus for memory
retrieval), and every sticker-reply sample with one
\texttt{intent\_label} from a 10-class taxonomy
(celebration\_approval, humor\_teasing, encouragement\_support, gratitude,
affection\_care, confusion\_curiosity, frustration\_disapproval,
disappointment\_sadness, surprise\_reaction, neutral\_acknowledgment) plus a
free-form \texttt{intent\_text}. The intent label feeds the auxiliary head
and induces the semantic-group label space; the intent text becomes the
third query stream.

\subsection{Multi-frame sticker encoding}

\texttt{.png} stickers are encoded as one-frame clips. \texttt{.gif} and
\texttt{.webm} are decoded to all frames (FFmpeg for video, PIL for GIF),
each frame is encoded by the OpenCLIP image encoder, and the per-frame
embeddings are mean-pooled and renormalised. During image-LoRA training,
the full bank is re-encoded once per epoch without gradients; for each batch
we additionally re-encode only the unique \emph{positive} stickers with
gradients (animated positives capped at 4 frames) and patch them into a
cloned bank, which keeps backprop tractable for a 42K-sticker bank.

\subsection{Tuning modes and experimental axes}

The code exposes two independent axes. The first is the memory strategy:
\texttt{retrieved\_topk} uses E5 retrieval over previous session summaries,
while \texttt{disabled} replaces the memory stream with a null placeholder.
This gives a clean memory ablation because the architecture, data split, and
training objective remain unchanged.

The second axis is the SFT mode. \textbf{direct\_clip} uses no learned head:
the query is just $f_T(c_t)$ scored against the frozen image bank, giving a
strong off-the-shelf VLM baseline. \textbf{head\_only} trains the retrieval
head on top of frozen OpenCLIP encoders. \textbf{text\_lora} additionally
injects LoRA ($r{=}8$, $\alpha{=}16$) into the text branch.
\textbf{image\_lora} injects LoRA into the visual branch only, while
\textbf{dual\_lora} adapts both branches. Only LoRA parameters and the head
are trainable; all original OpenCLIP weights are frozen.

%-------------------------------------------------------------------------
\section{Experiments}

\subsection{Setup}

We report the completed \texttt{rebuild\_medium} setting. The manifest is
constructed from 8{,}129 sessions and 368{,}503 provisional sticker-reply
samples. After frequency filtering and media decoding, the experiment uses
30{,}000 training samples, 5{,}000 validation samples (4{,}998 after media
filtering), and a 6{,}758-sticker bank over \texttt{.png}, \texttt{.gif}, and
\texttt{.webm}. All trainable models are optimized for 5 epochs with AdamW.
The retrieval head uses batch size 256; LoRA runs use batch size 96; inference
batch size is 256. The CLIP backbone is OpenCLIP ViT-B/32 with
\texttt{laion2b\_s34b\_b79k} weights. The retrieval temperature is $0.07$,
hidden dimension is 512, dropout is $0.1$, the auxiliary intent-loss weight
is $0.2$, and the intent vocabulary is clustered into 64 groups. LoRA uses
rank $r{=}8$, $\alpha{=}16$, dropout $0.05$, LoRA LR $10^{-4}$, and head LR
$10^{-3}$.

\noindent\textbf{Compared models.}
We report two experiments. Experiment~1 isolates memory. \texttt{direct\_clip}
is the context-only off-the-shelf baseline, while \texttt{head\_only} is
evaluated with memory disabled and with retrieved memory. This separates what
frozen CLIP already solves from the extra value of the memory-aware retriever.
Experiment~2 fixes \texttt{retrieved\_topk} memory and
compares \texttt{head\_only}, \texttt{text\_lora}, \texttt{image\_lora}, and
\texttt{dual\_lora}. This isolates whether SFT helps more on chat text,
sticker imagery, or both modalities together.

\subsection{Metrics}

We evaluate retrieval at two granularities. The primary metric is
\textbf{Group Hit@K}: a prediction is correct if any of the top-$K$ retrieved
stickers belongs to the same semantic intent group as the logged sticker. This
is the most faithful proxy for a chat sticker panel, where the interface shows
5--10 plausible choices and the user mainly needs the right affect, tone, or
communicative intent. We therefore treat \textbf{Group@5} and
\textbf{Group@10} as the north-star metrics. As a secondary diagnostic, we
also report \textbf{Exact Hit@K}, which requires recovering the single
observed sticker id. Exact metrics measure within-group ranking quality, but
are conservative because many unobserved stickers may be equally valid. All
Hit@K metrics are higher-is-better.

\subsection{Results}

\begin{table*}[t]
\setlength{\tabcolsep}{3.5pt}
\centering
\scriptsize
\begin{tabular}{l l l c c c c c c}
\toprule
\textbf{Model} & \textbf{Memory} & \textbf{Trainable}
 & \textbf{Group@5}$\uparrow$ & \textbf{Group@10}$\uparrow$
 & \textbf{Group@30}$\uparrow$ & \textbf{Exact@5}$\uparrow$
 & \textbf{Exact@30}$\uparrow$ & \textbf{MAP}$\uparrow$ \\
\midrule
\texttt{direct\_clip} & disabled & none
 & 0.8778 & 0.9078 & \best{0.9592} & 0.0012 & 0.0028 & 0.0013 \\
\texttt{head\_only} & disabled & head
 & 0.8567 & 0.8754 & 0.9324 & 0.0082 & 0.0412 & 0.0107 \\
\texttt{head\_only} & retrieved & head
 & 0.8619 & 0.8780 & 0.9406 & \best{0.0296} & \best{0.1263} & \best{0.0282} \\
\bottomrule
\end{tabular}
\caption{\textbf{Experiment 1: memory ablation and off-the-shelf baseline.}
Frozen CLIP is a sensible VLM baseline: it already retrieves semantically
related stickers, but does not solve exact sticker-id selection. The causal
memory ablation is \texttt{head\_only} disabled versus retrieved. With the
same head-only architecture, retrieved memory gives a large gain in
Exact@5/30, showing that memory is most useful for within-group ranking.}
\label{table:memory-ablation}
\end{table*}

\begin{table*}[t]
\setlength{\tabcolsep}{3.5pt}
\centering
\scriptsize
\begin{tabular}{l l c c c c c c c}
\toprule
\textbf{Model} & \textbf{Trainable}
 & \textbf{Group@1}$\uparrow$ & \textbf{Group@5}$\uparrow$
 & \textbf{Group@10}$\uparrow$ & \textbf{Group@30}$\uparrow$
 & \textbf{Exact@5}$\uparrow$ & \textbf{Exact@30}$\uparrow$ & \textbf{MAP}$\uparrow$ \\
\midrule
\texttt{direct\_clip} & none
 & 0.6335 & 0.8778 & \best{0.9084} & \best{0.9588} & 0.0012 & 0.0028 & 0.0013 \\
\texttt{head\_only} & head
 & \best{0.7933} & 0.8619 & 0.8780 & 0.9406 & 0.0296 & \second{0.1263} & 0.0282 \\
\texttt{text\_lora} & head+T
 & \second{0.7623} & 0.8661 & 0.8880 & 0.9420 & 0.0312 & 0.1190 & 0.0280 \\
\texttt{image\_lora} & head+V
 & 0.7063 & \second{0.8812} & 0.9044 & \second{0.9550} & \best{0.0376} & 0.1188 & \best{0.0302} \\
\texttt{dual\_lora} & head+T+V
 & 0.6275 & \best{0.8816} & \second{0.9064} & 0.9540 & \second{0.0326} & \best{0.1315} & \second{0.0286} \\
\bottomrule
\end{tabular}
\caption{\textbf{Experiment 2: modality-SFT results on \texttt{rebuild\_medium}.} The north-star
metrics are Group@5 and Group@10, matching a chat UI that displays a small
panel of sticker candidates. Exact metrics are stricter single-positive
diagnostics. \best{Red bold} marks the best method per column and
\second{blue bold} marks the runner-up. T/V denote text/visual LoRA.}
\label{table:sft-main}
\end{table*}

\begin{table*}[t]
\setlength{\tabcolsep}{5pt}
\centering
\scriptsize
\begin{tabular}{l l c c c c c}
\toprule
\textbf{Media} & \textbf{Model} & \textbf{Count}
 & \textbf{Group@5}$\uparrow$ & \textbf{Group@10}$\uparrow$
 & \textbf{Exact@5}$\uparrow$ & \textbf{Exact@30}$\uparrow$ \\
\midrule
PNG & \texttt{head\_only}  & 1{,}676 & 0.9141 & 0.9242 & 0.0197 & \best{0.1062} \\
PNG & \texttt{text\_lora}  & 1{,}676 & 0.9206 & 0.9314 & \second{0.0286} & \second{0.0990} \\
PNG & \texttt{image\_lora} & 1{,}676 & \second{0.9254} & \second{0.9391} & 0.0185 & 0.0752 \\
PNG & \texttt{dual\_lora}  & 1{,}676 & \best{0.9320} & \best{0.9403} & \best{0.0304} & 0.0973 \\
\midrule
GIF & \texttt{head\_only}  & 2{,}284 & 0.8695 & 0.8870 & \second{0.0499} & 0.1935 \\
GIF & \texttt{text\_lora}  & 2{,}284 & 0.8717 & 0.8971 & 0.0468 & 0.1843 \\
GIF & \texttt{image\_lora} & 2{,}284 & \best{0.8927} & \second{0.9168} & \best{0.0670} & \second{0.1948} \\
GIF & \texttt{dual\_lora}  & 2{,}284 & \second{0.8892} & \best{0.9177} & 0.0464 & \best{0.2062} \\
\midrule
WEBM & \texttt{head\_only}  & 1{,}038 & 0.7611 & 0.7832 & 0.0010 & 0.0106 \\
WEBM & \texttt{text\_lora}  & 1{,}038 & 0.7659 & 0.7977 & 0.0010 & 0.0077 \\
WEBM & \texttt{image\_lora} & 1{,}038 & \best{0.7842} & \second{0.8208} & \second{0.0039} & \best{0.0222} \\
WEBM & \texttt{dual\_lora}  & 1{,}038 & \second{0.7832} & \best{0.8266} & \best{0.0058} & \best{0.0222} \\
\bottomrule
\end{tabular}
\caption{\textbf{Per-media breakdown for trainable models.}
Direct CLIP is omitted because its result artifact does not store per-media
metrics. The group metrics are strongest and most stable in PNG/GIF, while
WEBM remains the hardest exact-retrieval format.}
\label{table:medium-media}
\end{table*}

\begin{figure}[t]
\centering
\setlength{\tabcolsep}{1pt}
\begin{minipage}[t]{0.495\linewidth}\centering
\includegraphics[width=\linewidth,trim=40 215 40 55,clip]%
  {demo_assets/dual_lora_png_demo/case_06_rank_2/case_06_rank_2_demo.pdf}\\[-1pt]
{\scriptsize (a)}
\end{minipage}\hfill
\begin{minipage}[t]{0.495\linewidth}\centering
\includegraphics[width=\linewidth,trim=40 215 40 55,clip]%
  {demo_assets/dual_lora_png_demo/case_26_rank_19/case_26_rank_19_demo.pdf}\\[-1pt]
{\scriptsize (b)}
\end{minipage}
\caption{\textbf{Qualitative examples (\texttt{dual\_lora}).}
(a) Near-success: the gold sticker is at rank~2 and all five candidates are
plausible \texttt{humor\_teasing} reactions, illustrating why Group@5 is
more informative than Exact@1 for sticker-panel UX. (b) Failure: gold rank
is 19; the model retrieves visually similar but tonally wrong stickers,
showing the limits of frozen-bank retrieval on long Game-domain disputes.
Six additional cases are listed in Appendix~\ref{app:cases}.}
\label{fig:case06}
\end{figure}

Table~\ref{table:memory-ablation} answers whether memory is useful, while
Table~\ref{table:sft-main} and Table~\ref{table:medium-media} answer which
SFT modality is best. Figure~\ref{fig:case06} shows a representative
\texttt{dual\_lora} retrieval on a real validation turn. The memory ablation shows that off-the-shelf CLIP is
already strong at coarse semantic matching, but retrieved memory is crucial
for exact sticker-id selection in the learned retriever. The SFT experiment shows that
\texttt{dual\_lora} is the best trained model under the panel-recommendation
objective: it wins overall Group@5 and is second only to the zero-shot
baseline on Group@10. At the same time, \texttt{image\_lora} has the strongest
exact top-5 ranking, suggesting that visual adaptation is especially useful
for ordering near-duplicate stickers within a semantic group.

%-------------------------------------------------------------------------
\section{Analysis and Discussion}

\noindent\textbf{Experiment 1: memory helps exact sticker selection.}
The off-the-shelf frozen CLIP baseline is a strong and necessary comparison:
with no training and no memory, it reaches Group@10 = 0.9078 and Group@30 =
0.9592. This means CLIP already understands the coarse visual-language
semantics of many sticker replies. However, it does not solve the actual
sticker-retrieval problem: Exact@30 is only 0.0028. The learned
\texttt{head\_only} model without memory improves Exact@30 to 0.0412, but
adding retrieved memory raises it further to 0.1263. Group metrics move much
less. The non-obvious lesson is therefore that memory is not mainly a coarse
semantic booster; it helps the model choose the concrete sticker id inside an
already plausible semantic neighborhood.

\noindent\textbf{Experiment 2: visual and dual SFT answer different needs.}
Once memory is fixed to \texttt{retrieved\_topk}, the SFT question is which
OpenCLIP branch to adapt. For a chat panel, semantic Group@5/10 should be the
north star because users see multiple plausible stickers. Under that criterion,
\texttt{dual\_lora} is the best trained model, with Group@5 = 0.8816 and
trained-model Group@10 = 0.9064. For stricter within-group ranking,
\texttt{image\_lora} is strongest: it has the best Exact@5 and MAP, suggesting
that adapting the visual branch is especially useful for separating visually
similar stickers and animated variants. Thus dual LoRA is the better first-stage
retriever, while image LoRA is the strongest reference for exact top-5 ranking.

\noindent\textbf{Why modalities behave differently.}
The per-media table shows that GIF is the most responsive format to visual
adaptation: \texttt{image\_lora} wins GIF Exact@5 and Group@5, while
\texttt{dual\_lora} wins GIF Exact@30 and Group@10. Animated stickers encode
much of their intent in character pose, motion, and style, so adapting the
visual branch makes the embedding space more sticker-specific. PNG behaves
differently. The frozen CLIP space already separates many static stickers
well, so \texttt{head\_only} remains strong on PNG Exact@30, while
\texttt{dual\_lora} gives the best PNG Group@5/10. WEBM remains the hardest
exact-retrieval case; frame sampling and video compression likely erase cues
needed to distinguish close variants.

\noindent\textbf{Recommended retrieval design.}
The two experiments suggest a two-stage design. First, keep retrieved memory
because it materially improves exact selection over a no-memory head. Second,
use \texttt{dual\_lora} as the group-aware first-stage retriever and optimize
around Group@5/10. Exact@5 and MAP should be tracked as secondary ranking
signals, where \texttt{image\_lora} currently sets the strongest reference
point. A future reranker can combine these strengths: use \texttt{dual\_lora}
to guarantee semantic coverage, then apply a lightweight visual/textual
reranker or diversity rule to choose the final 5--10 stickers.

%-------------------------------------------------------------------------
\section{Conclusion}

We presented MultiSticker, an intent-guided dialogue-aware sticker
retrieval system that combines OpenCLIP, retrieved long-term memory, and
LLM-generated intent into a trainable retrieval head with optional LoRA
adaptation. The report is organized as two experiments. The memory experiment
shows that a strong frozen CLIP baseline already finds semantic neighbors, but
retrieved dialogue memory is what substantially improves exact sticker-id
selection. The modality-SFT experiment shows that dual LoRA is the strongest
trained model for group-level panel recommendation, while image-side LoRA gives
the best exact top-5 ordering. Together, these results support a two-stage view
of sticker retrieval: keep memory for fine-grained selection, optimize the
first-stage retriever for semantically correct 5--10 item panels, and use exact
metrics as diagnostics for reranking.

\noindent
{\bf Acknowledgments.} We thank the authors of OpenCLIP and PEFT, and the
U-Sticker maintainers. Session summaries and intent annotations were
produced with Qwen2.5-32B-GPTQ; all experimental decisions are our own.

{\small
\bibliographystyle{ieee}
\bibliography{egbib}
}

\appendix
\section{Additional qualitative cases}
\label{app:cases}
Six additional rendered demos (chat bubble + retrieved top-5 panel +
session memory + intent) accompany the case shown in
Figure~\ref{fig:case06}. They span a success$\to$near-miss$\to$failure
gradient across the Game and Media-Sharing domains and the
\texttt{humor\_teasing}, \texttt{neutral\_acknowledgment}, and
\texttt{frustration\_disapproval} intent groups:

\begin{itemize}\itemsep1pt\parsep0pt
\item \href{demo_assets/dual_lora_png_demo/case_01_rank_1/case_01_rank_1_demo.pdf}{Case~01} — Media Sharing, \texttt{humor\_teasing}, gold rank~1 (success).
\item \href{demo_assets/dual_lora_png_demo/case_09_rank_3/case_09_rank_3_demo.pdf}{Case~09} — Game, \texttt{neutral\_acknowledgment}, gold rank~3.
\item \href{demo_assets/dual_lora_png_demo/case_10_rank_4/case_10_rank_4_demo.pdf}{Case~10} — Game, \texttt{frustration\_disapproval}, gold rank~4.
\item \href{demo_assets/dual_lora_png_demo/case_14_rank_9/case_14_rank_9_demo.pdf}{Case~14} — Game, \texttt{frustration\_disapproval}, gold rank~9.
\item \href{demo_assets/dual_lora_png_demo/case_15_rank_11/case_15_rank_11_demo.pdf}{Case~15} — Game, \texttt{frustration\_disapproval}, gold rank~11 (near-miss).
\item \href{demo_assets/dual_lora_png_demo/case_27_rank_20/case_27_rank_20_demo.pdf}{Case~27} — Game, \texttt{neutral\_acknowledgment}, gold rank~20 (failure).
\end{itemize}

The PDFs live under
\path{Latex_report/demo_assets/dual_lora_png_demo/<case>/} in the source
release.

\clearpage
\onecolumn
\begin{figure}[p]
\centering
\includegraphics[page=1,width=0.96\textwidth]%
  {demo_assets/dual_lora_png_demo/case_01_rank_1/case_01_rank_1_demo.pdf}
\caption{\textbf{Appendix demo: Case~01.} Media Sharing,
\texttt{humor\_teasing}, gold rank~1.}
\label{fig:app-case01}
\end{figure}

\begin{figure}[p]
\centering
\includegraphics[page=1,width=0.96\textwidth]%
  {demo_assets/dual_lora_png_demo/case_09_rank_3/case_09_rank_3_demo.pdf}
\caption{\textbf{Appendix demo: Case~09.} Game,
\texttt{neutral\_acknowledgment}, gold rank~3.}
\label{fig:app-case09}
\end{figure}

\begin{figure}[p]
\centering
\includegraphics[page=1,width=0.96\textwidth]%
  {demo_assets/dual_lora_png_demo/case_10_rank_4/case_10_rank_4_demo.pdf}
\caption{\textbf{Appendix demo: Case~10.} Game,
\texttt{frustration\_disapproval}, gold rank~4.}
\label{fig:app-case10}
\end{figure}

\begin{figure}[p]
\centering
\includegraphics[page=1,width=0.96\textwidth]%
  {demo_assets/dual_lora_png_demo/case_14_rank_9/case_14_rank_9_demo.pdf}
\caption{\textbf{Appendix demo: Case~14.} Game,
\texttt{frustration\_disapproval}, gold rank~9.}
\label{fig:app-case14}
\end{figure}

\begin{figure}[p]
\centering
\includegraphics[page=1,width=0.96\textwidth]%
  {demo_assets/dual_lora_png_demo/case_15_rank_11/case_15_rank_11_demo.pdf}
\caption{\textbf{Appendix demo: Case~15.} Game,
\texttt{frustration\_disapproval}, gold rank~11.}
\label{fig:app-case15}
\end{figure}

\begin{figure}[p]
\centering
\includegraphics[page=1,width=0.96\textwidth]%
  {demo_assets/dual_lora_png_demo/case_27_rank_20/case_27_rank_20_demo.pdf}
\caption{\textbf{Appendix demo: Case~27.} Game,
\texttt{neutral\_acknowledgment}, gold rank~20.}
\label{fig:app-case27}
\end{figure}

\end{document}
